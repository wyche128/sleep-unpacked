import React from 'react';
import TopPicksCategoryPage from '../../../components/TopPicksCategoryPage';

export const metadata = {
    title: 'Best Value Mattress | SleepUnpacked',
    description: 'The best value pick that still clears our durability baseline, plus alternatives based on Living Reviews.',
};

export default function BestValueMattressPage() {
    return <TopPicksCategoryPage slug="best-value-mattress" />;
}

